import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    wrapper: { height: 250, maxHeight: 250 },
    contentContainer: { alignItems: 'center' },
    separator: { width: 10 },
});
